inventory.connect.url=http://localhost:${wiremock.server.port}
# 'wiremock.server.port' is a special variable which gets auto-initialized while running tests.
# Hence we need not specify an explicit value for it.